id: speedmap

requirements:
  - name: ecoscope-workflows-core
    version: ">=0.22.17, <0.23.0"
    channel: https://repo.prefix.dev/ecoscope-workflows/

  - name: ecoscope-workflows-ext-ecoscope
    version: ">=0.22.17, <0.23.0"
    channel: https://repo.prefix.dev/ecoscope-workflows/

  - name: ecoscope-workflows-ext-custom
    version: "0.0.41.*"
    channel: https://repo.prefix.dev/ecoscope-workflows-custom/

  - name: ecoscope-workflows-ext-gamm-trend-analysis
    version: "0.0.1"
    channel: https://repo.prefix.dev/ecoscope-workflows-custom/

rjsf-overrides:
  $defs:
    ValueGrouper.properties.index_name.oneOf:
      - const: subject_name
        title: Subject Name
      - const: subject_subtype
        title: Subject Subtype
      - const: subject_sex
        title: Subject Sex

    DefaultLabels.properties.label_ranges.default: true
    DefaultLabels.properties.label_decimals.default: 1
    DefaultLabels.properties.label_suffix.default: " km/h"

  properties:
    Subject Group.properties.subject_obs.properties.subject_group_name.ecoscope:transform:
      - transformer: EarthRangerEnumResolver
        transformer_kws:
          type: subject_group_name
          depends_on: properties.er_client_name.properties.data_source.properties.name

    Group Data.properties.groupers.properties.groupers.ecoscope:transform:
      - transformer: EarthRangerEnumResolver
        transformer_kws:
          type: spatial_feature_group
          depends_on: properties.er_client_name.properties.data_source.properties.name
    Group Data.properties.groupers.properties.groupers.items.anyOf:
      - $ref: "#/$defs/TemporalGrouper"
      - $ref: "#/$defs/ValueGrouper"

    time_range.properties.since.default: "2008-01-01T00:00:00.000Z"
    time_range.properties.since.description: Start of the movement analysis period.
    time_range.properties.until.description: End of the movement analysis period.

    Trend Analysis.properties.gamm_model.properties.degree_of_freedom.default: 3
    Trend Analysis.properties.gamm_model.properties.degree_of_freedom.description: >-
      Degrees of freedom for the trend spline. Lower this if the fit fails
      (e.g. for a group with few monthly data points); raise it for a smoother
      fit on longer time series with more data points. Must be greater than
      Degree.

    Trend Analysis.properties.gamm_model.properties.degree.default: 2
    Trend Analysis.properties.gamm_model.properties.degree.description: >-
      Degree of the trend spline (2 = quadratic). Lower values fit more
      reliably on groups with few monthly data points; degree must be at
      least 2 for the smoothing penalty to be computed.

    Trend Analysis.properties.gamm_model.properties.optimize_alpha.default: false
    Trend Analysis.properties.gamm_model.properties.optimize_alpha.description: >-
      Automatically search for the best smoothing parameter. The automatic
      search can fail ("Perfect separation detected") on groups with very
      few monthly data points, so this defaults to off with a fixed Alpha
      value instead; turn it on for a data-driven fit on larger groups.
    Trend Analysis.properties.gamm_model.properties.alpha.default: 1.0
    Trend Analysis.properties.gamm_model.properties.alpha.description: >-
      Fixed smoothing parameter, used only when Optimize Alpha is off.

    Dashboard.properties.map_widget_title.properties.title.title: Map Title
    Dashboard.properties.map_widget_title.properties.title.default: Subject Speed Map
    Dashboard.properties.map_widget_title.properties.title.description: >-
      Title displayed on the speed map.

    Dashboard.properties.chart_widget_title.properties.title.title: Trend Chart Title
    Dashboard.properties.chart_widget_title.properties.title.default: Mean Speed Trend Chart
    Dashboard.properties.chart_widget_title.properties.title.description: >-
      Title displayed on the GAMM speed trend chart.

task-instance-defaults:
  skipif:
    conditions:
      - any_is_empty_df
      - any_dependency_skipped

workflow:
  - name: Set Workflow Details
    id: workflow_details
    task: set_workflow_details

  - name: Time Range
    id: time_range
    task: set_time_range
    partial:
      time_format: "%d %b %Y %H:%M:%S %Z"

  - name: Extract Timezone Selection
    id: get_timezone
    task: get_timezone_from_time_range
    partial:
      time_range: ${{ workflow.time_range.return }}

  - name: Data Source
    id: er_client_name
    task: set_er_connection

  - title: Subject Group
    type: task-group
    description: Choose the EarthRanger subject group to map and analyze.
    tasks:
      - name: ""
        id: subject_obs
        task: get_subjectgroup_observations
        partial:
          client: ${{ workflow.er_client_name.return }}
          time_range: ${{ workflow.time_range.return }}
          raise_on_empty: false
          include_details: false
          include_subjectsource_details: false
          filter: clean

  - name: Convert to timezone
    id: convert_to_user_timezone
    task: convert_values_to_timezone
    partial:
      df: ${{ workflow.subject_obs.return }}
      timezone: ${{ workflow.get_timezone.return }}
      columns:
        - fixtime

  - title: Group Data
    type: task-group
    description: Optionally group the data by subject, time period, or spatial feature group.
    tasks:
      - id: groupers
        task: set_groupers

      - id: spatial_group_ids
        task: extract_spatial_grouper_feature_group_names
        partial:
          groupers: ${{ workflow.groupers.return }}

      - id: fetch_spatial_feature_groups
        task: get_spatial_features_group
        partial:
          client: ${{ workflow.er_client_name.return }}
        map:
          argnames: spatial_features_group_name
          argvalues: ${{ workflow.spatial_group_ids.return }}

      - id: resolved_groupers
        task: resolve_spatial_feature_groups_for_spatial_groupers
        partial:
          groupers: ${{ workflow.groupers.return }}
          spatial_feature_groups: ${{ workflow.fetch_spatial_feature_groups.return }}

  - name: Transform Observations to Relocations
    id: subject_reloc
    task: process_relocations
    partial:
      observations: ${{ workflow.convert_to_user_timezone.return }}
      relocs_columns:
        - groupby_col
        - fixtime
        - junk_status
        - geometry
        - extra__subject__name
        - extra__subject__subject_subtype
        - extra__subject__sex
      filter_point_coords:
        - x: 180.000000
          y: 90.000000
        - x: 0.000000
          y: 0.000000
        - x: 1.000000
          y: 1.000000

  - name: Convert Relocations to Trajectory
    id: subject_traj
    task: relocations_to_trajectory
    partial:
      relocations: ${{ workflow.subject_reloc.return }}

  - name: ""
    id: traj_add_temporal_index
    task: add_temporal_index
    partial:
      df: ${{ workflow.subject_traj.return }}
      time_col: segment_start
      groupers: ${{ workflow.resolved_groupers.return }}
      cast_to_datetime: true
      format: mixed

  - name: ""
    id: traj_add_spatial_index
    task: add_spatial_index
    partial:
      gdf: ${{ workflow.traj_add_temporal_index.return }}
      groupers: ${{ workflow.resolved_groupers.return }}

  - name: ""
    id: rename_traj_columns
    task: map_columns
    partial:
      df: ${{ workflow.traj_add_spatial_index.return }}
      drop_columns: []
      retain_columns: []
      rename_columns:
        extra__name: subject_name
        extra__subject_subtype: subject_subtype
        extra__sex: subject_sex
      raise_if_not_found: true

  - name: ""
    id: classify_traj_speed
    task: apply_classification
    partial:
      df: ${{ workflow.rename_traj_columns.return }}
      input_column_name: speed_kmhr
      output_column_name: speed_bins
      classification_options:
        scheme: equal_interval
        k: 6

  - name: ""
    id: split_subject_traj_groups
    task: split_groups
    partial:
      df: ${{ workflow.classify_traj_speed.return }}
      groupers: ${{ workflow.resolved_groupers.return }}

  - name: Base Maps
    id: base_map_defs
    task: set_base_maps_pydeck

  - name: ""
    id: sort_traj_speed
    task: sort_values
    partial:
      column_name: speed_bins
      ascending: true
      na_position: last
    mapvalues:
      argnames: df
      argvalues: ${{ workflow.split_subject_traj_groups.return }}

  - name: ""
    id: colormap_traj_speed
    task: apply_color_map
    partial:
      input_column_name: speed_bins
      output_column_name: speed_bins_colormap
      colormap:
        - "#1a9850"
        - "#91cf60"
        - "#d9ef8b"
        - "#fee08b"
        - "#fc8d59"
        - "#d73027"
    mapvalues:
      argnames: df
      argvalues: ${{ workflow.sort_traj_speed.return }}

  - name: ""
    id: traj_map_layers
    task: create_path_layer
    partial:
      layer_style:
        get_color: speed_bins_colormap
      legend:
        title: Speed
        label_column: speed_bins
        color_column: speed_bins_colormap
    mapvalues:
      argnames: geodataframe
      argvalues: ${{ workflow.colormap_traj_speed.return }}

  - name: ""
    id: traj_ecomap
    task: draw_map
    partial:
      tile_layers: ${{ workflow.base_map_defs.return }}
      legend_style:
        placement: bottom-right
      static: false
      title: null
      max_zoom: 20
    mapvalues:
      argnames: geo_layers
      argvalues: ${{ workflow.traj_map_layers.return }}

  - name: ""
    id: ecomap_html_urls
    task: persist_text
    partial:
      root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
    mapvalues:
      argnames: text
      argvalues: ${{ workflow.traj_ecomap.return }}

  - name: ""
    id: traj_add_month_index
    task: add_temporal_index
    partial:
      time_col: segment_start
      groupers:
        - temporal_index: "%Y-%m"
      cast_to_datetime: true
      format: mixed
    mapvalues:
      argnames: df
      argvalues: ${{ workflow.split_subject_traj_groups.return }}

  - name: ""
    id: speed_trends
    task: summarize_df
    partial:
      summary_params:
        - display_name: mean_speed_kmhr
          aggregator: mean
          column: speed_kmhr
        - display_name: period_start
          aggregator: min
          column: segment_start
          decimal_places: null
      groupby_cols:
        - TemporalGrouper_%Y-%m
      reset_index: true
    mapvalues:
      argnames: df
      argvalues: ${{ workflow.traj_add_month_index.return }}

  - name: ""
    id: persist_speed_trend_data
    task: persist_df_wrapper
    skipif:
      conditions:
        - never
    partial:
      root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
      sanitize: true
      filename_prefix: speed_trends
      filetypes:
        - parquet
    mapvalues:
      argnames: df
      argvalues: ${{ workflow.speed_trends.return }}

  - title: Trend Analysis
    type: task-group
    description: Configure the GAM trend fitting parameters for mean speed over time.
    tasks:
      - name: ""
        id: gamm_model
        task: fit_gamm_model
        partial:
          time_column: period_start
          value_column: mean_speed_kmhr
          lower_bound: null
          upper_bound: null
        mapvalues:
          argnames: dataframe
          argvalues: ${{ workflow.speed_trends.return }}

      - name: ""
        id: trend_predictions
        task: predict_gamm_trends
        partial:
          time_values: null
          include_ci: true
        mapvalues:
          argnames: model_params
          argvalues: ${{ workflow.gamm_model.return }}

      - name: ""
        id: trend_time_cast
        task: add_temporal_index
        partial:
          groupers: {}
          time_col: time
          cast_to_datetime: true
          format: mixed
        mapvalues:
          argnames: df
          argvalues: ${{ workflow.trend_predictions.return }}

      - name: ""
        id: persist_trend_data
        task: persist_df_wrapper
        skipif:
          conditions:
            - never
        partial:
          root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
          sanitize: true
          filename_prefix: trend_predictions
          filetypes:
            - parquet
        mapvalues:
          argnames: df
          argvalues: ${{ workflow.trend_time_cast.return }}

      - name: Trend Chart
        id: speed_trend_chart
        task: draw_historic_timeseries
        partial:
          current_value_column: y
          time_column: time
          current_value_title: Observed Mean Speed
          historic_min_column: ci_lower
          historic_max_column: ci_upper
          historic_mean_column: predicted
          historic_mean_title: Trend Estimate
          historic_band_title: Trend Confidence
          layout_style:
            xaxis:
              title: Time
            yaxis:
              title: Mean Speed (km/h)
          upper_lower_band_style:
            mode: lines
            line:
              color: green
          historic_mean_style: null
          current_value_style: null
        mapvalues:
          argnames: dataframe
          argvalues: ${{ workflow.trend_time_cast.return }}

      - name: ""
        id: persist_speed_trend_chart
        task: persist_text
        partial:
          root_path: ${{ env.ECOSCOPE_WORKFLOWS_RESULTS }}
        mapvalues:
          argnames: text
          argvalues: ${{ workflow.speed_trend_chart.return }}

  - title: Dashboard
    type: task-group
    description: Configure the dashboard output titles.
    tasks:
      - name: ""
        id: map_widget_title
        task: set_title_var

      - name: ""
        id: chart_widget_title
        task: set_title_var

  - name: ""
    id: speed_map_widget
    task: create_map_widget_single_view
    skipif:
      conditions:
        - never
    partial:
      title: ${{ workflow.map_widget_title.return }}
    map:
      argnames: [view, data]
      argvalues: ${{ workflow.ecomap_html_urls.return }}

  - name: ""
    id: grouped_speed_map
    task: merge_widget_views
    skipif:
      conditions:
        - never
    partial:
      widgets: ${{ workflow.speed_map_widget.return }}

  - name: ""
    id: speed_trend_widget
    task: create_plot_widget_single_view
    skipif:
      conditions:
        - never
    partial:
      title: ${{ workflow.chart_widget_title.return }}
    map:
      argnames: [view, data]
      argvalues: ${{ workflow.persist_speed_trend_chart.return }}

  - name: ""
    id: grouped_speed_trend
    task: merge_widget_views
    skipif:
      conditions:
        - never
    partial:
      widgets: ${{ workflow.speed_trend_widget.return }}

  - name: ""
    id: speedmap_dashboard
    task: gather_dashboard
    partial:
      details: ${{ workflow.workflow_details.return }}
      time_range: ${{ workflow.time_range.return }}
      widgets:
        - ${{ workflow.grouped_speed_map.return }}
        - ${{ workflow.grouped_speed_trend.return }}
      groupers: ${{ workflow.resolved_groupers.return }}
