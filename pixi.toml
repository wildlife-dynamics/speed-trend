[project]
name = "speedmap-trend"
channels = [
  # Uncomment the following lines to use local channels for development.
  # "file:///tmp/ecoscope-workflows/release/artifacts/",
  # "file:///tmp/ecoscope-workflows-custom/release/artifacts/",
  'https://repo.prefix.dev/ecoscope-workflows/',
  'https://repo.prefix.dev/ecoscope-workflows-custom/',
  'conda-forge',
]
platforms = ['linux-64', 'osx-64', 'osx-arm64', 'win-64']

[dependencies]
curl = "*"
rattler-build = "*"
yq = "*"
hatch = "*"

[feature.compile.dependencies]
# Include this if you add new tasks and the package has been built
ecoscope-workflows-core = ">=0.22.17, <0.23.0"
ecoscope-workflows-ext-custom = "0.0.41.*"
ecoscope-workflows-ext-ecoscope = ">=0.22.17, <0.23.0"
ecoscope-workflows-ext-speedmap-trend = "*"


[feature.compile.pypi-dependencies]
# Note: you can mark your task libraries editable here.
# # Remember to comment out duplicate dependencies in [feature.compile.dependencies] above
# ecoscope-workflows-ext-speedmap-trend = { path = "src/ecoscope-workflows-ext-speedmap-trend", editable = true }

[environments]
compile = { features = [
  "compile",
], solve-group = "compile", no-default-feature = true }

[tasks]
[tasks.build-release]
cmd = './publish/build.sh'

[tasks.pytest-310]
cmd = './dev/pytest-task.sh 310'

[tasks.pytest-311]
cmd = './dev/pytest-task.sh 311'

[tasks.pytest-312]
cmd = './dev/pytest-task.sh 312'

[feature.compile.tasks.compile-speedmap]
cmd = './dev/recompile.sh speedmap --install'
depends-on = ["build-release"]

[feature.compile.tasks.recompile-speedmap]
cmd = './dev/recompile.sh speedmap --update'
depends-on = ["build-release"]
